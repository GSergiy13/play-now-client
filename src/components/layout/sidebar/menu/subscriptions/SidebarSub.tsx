const SidebarSub = () => {
	return (
		<div>
			<ul>
				{/* {items.map(menuItem => (
					<SubItem
						key={menuItem.label}
						item={menuItem}
					/>
				))} */}
			</ul>
		</div>
	)
}

export default SidebarSub
